/* eslint-disable no-unused-vars */
import React from 'react';
import useTitle from '../../Title/Title';

const Blog = () => {
    useTitle("Blog");
    return (
        <div>
            blog is coming
        </div>
    );
};

export default Blog;